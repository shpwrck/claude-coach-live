import{t as e}from"./report-card-DSq-8EKU.js";import{a as t,i as n,n as r,r as i,t as a}from"./pricing-BMlwVQF4.js";import{n as o,t as s}from"./parse-sessions-CCBmtGyZ.js";import{n as c}from"./parse-runtime-events-Drlhy0bx.js";import{t as l}from"./parse-agent-effectiveness-CnO1L5X0.js";import{o as u,r as d}from"./parse-tools-ClTVSTcT.js";import{t as f}from"./parse-tool-effectiveness-DJGlwOxt.js";import{a as ee}from"./cost-attribution-D0SNE3Qk.js";import{t as te}from"./parse-tool-inventory-CQNelDRf.js";import{i as ne,n as re,r as ie,s as ae,t as p}from"./context-health-Cpv88zSb.js";import{n as m,r as oe,t as se}from"./parse-compaction-risk-NUfRSfQ5.js";import{a as ce,i as le}from"./parse-files-CyCZ1IDv.js";import"./parse-tasks-C4SjXZ01.js";import{i as ue,o as de,r as fe}from"./parse-permissions-WKlEqxPM.js";import{a as pe,r as me}from"./parse-errors-D0e9-JHl.js";import{t as he}from"./parse-stats-cache-BKCbfc9d.js";var h={critical:3,warning:2,info:1};function g(e){return e===`info`?`warning`:`critical`}var ge=.2,_e=new Set([`429`,`529`]),_=e=>e.slice(0,8);function v(e,t,n){let r=e?.permissions?.[t];if(!Array.isArray(r)||r.length===0)return!1;let i=new Set(r);return n.every(e=>i.has(e))}function y(e){let t=/^([A-Za-z][\w-]*)\((.*)\)$/.exec(e.trim());return t?{tool:t[1],specifier:t[2]}:{tool:e.trim(),specifier:null}}function b(e){return e.endsWith(`:*`)?{literal:e.slice(0,-2).trim(),prefix:!0}:{literal:e.trim(),prefix:!1}}function ve(e,t){let{tool:n,specifier:r}=y(e);if(t.toolName!==n)return!1;if(r===null)return!0;if(n===`Bash`){let e=typeof t.input?.command==`string`?t.input.command.trim():``;if(!e)return!1;let{literal:n,prefix:i}=b(r);return i?e===n||e.startsWith(n+` `):e===n}return null}function ye(e,t){let n=y(e),r=y(t);if(n.tool!==r.tool)return!1;if(r.specifier===null)return!0;if(n.specifier===null)return!1;if(n.tool===`Bash`){let e=b(n.specifier),t=b(r.specifier);return t.prefix?e.literal===t.literal||e.literal.startsWith(t.literal+` `):!e.prefix&&e.literal===t.literal}return n.specifier===r.specifier}var be=[`rm`,`rmdir`,`dd`,`mkfs`,`shred`,`curl`,`wget`,`sudo`,`chmod`,`chown`,`chgrp`,`kill`,`pkill`,`killall`,`shutdown`,`reboot`,`halt`,`fdisk`,`parted`,`mkswap`,`git reset --hard`,`git clean`,`git push --force`,`git push -f`,`git push --force-with-lease`];function xe(e){let{tool:t,specifier:n}=y(e);if(t!==`Bash`||n===null)return!1;let{literal:r}=b(n),i=r.toLowerCase();return be.some(e=>i===e||i.startsWith(e+` `))}function x(e){let t=e?.hooks?.PostToolUse;return Array.isArray(t)?t.some(e=>{let t=typeof e?.matcher==`string`?e.matcher:``;return/\bEdit\b|\bWrite\b/.test(t)}):!1}function Se(e){return typeof e?.model==`string`&&e.model.length>0}function S(e){return typeof e?.model==`string`&&/\bhaiku\b/i.test(e.model)}function C(e){if(!e?.claudeMd)return``;let t=[];e.claudeMd.global&&t.push(e.claudeMd.global);for(let n of Object.values(e.claudeMd.perProject??{}))typeof n==`string`&&n.length>0&&t.push(n);return t.join(`

`)}function w(e,t){if(!t)return!1;let n=Array.isArray(t.headings)&&t.headings.length>0?t.headings:null,r=Array.isArray(t.bodyPhrases)&&t.bodyPhrases.length>0?t.bodyPhrases:null;if(!n&&!r)return!1;let i=C(e);if(i.length===0)return!1;if(n){let e=i.split(`
`).filter(e=>/^#{1,6}\s+/.test(e));if(!n.some(t=>e.some(e=>t.test(e))))return!1}if(r){let e=i.toLowerCase();if(!r.every(t=>e.includes(t.toLowerCase())))return!1}return!0}var Ce=[`Bash(rm -rf:*)`,`Bash(rm -fr:*)`,`Bash(git reset --hard:*)`,`Bash(git clean -fd:*)`,`Bash(git push --force:*)`,`Bash(git push -f:*)`,`Bash(dd:*)`,`Bash(mkfs:*)`,`Bash(shred:*)`,`Bash(curl:*)`,`Bash(wget:*)`],we=[`Bash(rm -rf:*)`,`Bash(rm -fr:*)`,`Bash(git reset --hard:*)`,`Bash(git clean -fd:*)`,`Bash(git push --force:*)`,`Bash(git push -f:*)`,`Bash(dd:*)`,`Bash(mkfs:*)`,`Bash(shred:*)`],T=[`Bash(ls:*)`,`Bash(pwd)`,`Bash(echo:*)`,`Bash(git status:*)`,`Bash(git diff:*)`,`Bash(git log:*)`,`Bash(git show:*)`];function E(e){return isFinite(e)?e>0&&e<.01?`$${e.toFixed(4)}`:`$${e.toFixed(2)}`:`$0.00`}function D(e){let t=e.lastIndexOf(`/`);return t===-1?e:e.slice(t+1)}function Te(e,t){let n=e.replace(/\s+/g,` `).trim();return n.length>t?n.slice(0,t)+`…`:n}function Ee(e,t){return Math.max(0,Math.round((t-e)/(1440*60*1e3)))}function De(e){let t=0,n=0;for(let r of e){let e=s(r);n+=e,o(r.entrypoint)&&(t+=e)}let r=n>0?t/n*100:0;return{autoCost:t,total:n,share:r}}var Oe={headings:[/^##\s+Cach(e|ing)\b/i],bodyPhrases:[`1-hour cache`,`5-minute`]},ke={id:`cost.cache-1h-waste`,category:`cost`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(w(e.liveConfig,Oe))return null;let t=0,r=0,i=new Set;for(let a of e.tokenData)for(let e of a.entries){let o=Math.min(e.cacheCreation1hTokens,e.cacheCreationTokens);if(o<=0)continue;let s=n(e.model);t+=o/1e6*(s.cacheWrite1h-s.cacheWrite5m),r+=o,i.add(a.sessionId)}return t<.05?null:{id:`cost.cache-1h-waste`,category:`cost`,severity:t>=1?`warning`:`info`,title:`Reduce 1-hour cache writes`,detail:`${(r/1e6).toFixed(2)}M tokens were written to the 1-hour cache across ${i.size} session(s), billed at 2× input vs 1.25× for the default 5-minute cache.`,action:`If that context is not reused beyond 5 minutes, prefer the 5-minute cache — up to the amount shown is recoverable.`,estSavingsUsd:t,affected:i.size,view:`tokens`,fix:{target:`CLAUDE.md`,label:`Note the 1h-cache tradeoff`,note:`Paste into CLAUDE.md so sessions avoid the 2× cache write when context is short-lived. No settings key controls cache TTL.`,snippet:`## Caching
- Prefer the default 5-minute prompt cache for routine work; it is billed at 1.25× input vs 2× for the 1-hour cache.
- Only rely on the 1-hour cache for context genuinely reused across gaps >5 min. For short, scoped tasks, finish or compact before idle gaps so context is not re-written to the pricier 1h cache.`,appliedMarkers:Oe}}}},Ae={headings:[/^##\s+Session scope\b/i],bodyPhrases:[`one session to one task`]},je={id:`cost.expensive-sessions`,category:`cost`,dataDeps:[`tokenData`,`toolData`,`liveConfig`],rule(e){if(w(e.liveConfig,Ae))return null;let t=ee(e.tokenData,e.toolData,5),n=e.tokenData.reduce((e,t)=>e+s(t),0),r=t.slice(0,3),i=r.reduce((e,t)=>e+t.estimatedCost,0);if(n<=0||i<.05)return null;let a=i/n*100;return a<25||e.tokenData.length<5?null:{id:`cost.expensive-sessions`,category:`cost`,severity:`info`,title:`Spend is concentrated in a few sessions`,detail:`The top 3 sessions account for ${a.toFixed(0)}% of total estimated spend (${E(i)} of ${E(n)}).`,action:`Review these sessions for runaway context or repeated work that could be scoped down or split.`,estSavingsUsd:i,affected:r.length,evidence:r.map(e=>`${_(e.sessionId)}, ${E(e.estimatedCost)}, ${e.topTool??`no tools`}`),view:`cost`,fix:{target:`CLAUDE.md`,label:`Add session-scoping guidance`,note:`Paste into CLAUDE.md to curb the runaway-context pattern behind the top sessions.`,snippet:`## Session scope
- Keep one session to one task; start a fresh session when the task changes so stale context is not re-sent and re-billed each turn.
- Compact or clear once a sub-task is done rather than letting context grow unbounded.
- Avoid re-reading large files or directories repeatedly in a single session; reference them once.`,appliedMarkers:Ae}}}},Me={id:`cost.automation-share`,category:`cost`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(S(e.liveConfig?.settings))return null;let{autoCost:n,total:r,share:s}=De(e.tokenData),c=0,l=new Set;for(let n of e.tokenData)if(o(n.entrypoint)){l.add(n.sessionId);for(let e of n.entries){let n=e.model||`unknown`;if(t(n).isSynthetic)continue;let r=i(e,n)-i(e,a);r>0&&(c+=r)}}if(n<1||r<=0||s<15)return null;let u=c>=.01?` Running those automation turns on Haiku instead would have cost about ${E(c)} less.`:``;return{id:`cost.automation-share`,category:`cost`,severity:`info`,title:`Automation drives a large share of spend`,detail:`Automated (sdk-*) sessions account for ${E(n)} (${s.toFixed(0)}% of total) across ${l.size} session(s).${u}`,action:`Confirm automated runs use the cheapest model that meets the quality bar — Haiku/Sonnet often suffice for scripted work.`,estSavingsUsd:c,affected:l.size,view:`cost`,fix:{target:`settings.json`,label:`Default automation to Haiku`,note:`Merge into the settings.json your sdk-* runs use, to right-size the ${s.toFixed(0)}% automated spend. Use the cheapest model that holds your quality bar.`,snippet:`{
  "model": "claude-haiku-4-5"
}`}}}},Ne={id:`cost.unknown-model`,category:`cost`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(Se(e.liveConfig?.settings))return null;let t=e.tokenData.filter(e=>e.hasUnknownModel);return t.length===0?null:{id:`cost.unknown-model`,category:`cost`,severity:`info`,title:`Some cost estimates are guesses`,detail:`${t.length} session(s) used a model string not in the pricing table and fell back to Sonnet-tier rates.`,action:`Treat those sessions’ cost figures as approximate; add the model to the pricing table for accuracy.`,affected:t.length,view:`tokens`,fix:{target:`settings.json`,label:`Pin a known model`,note:`Set an explicit model in settings.json so future sessions are priced from a known rate instead of the Sonnet-tier fallback used for the ${t.length} guessed session(s).`,snippet:`{
  "model": "claude-sonnet-4-6"
}`}}}},O=n(`claude-opus-4-1-20250414`),Pe=n(`claude-opus-4-7`),Fe=O.input-Pe.input,Ie=.5,Le={id:`cost.legacy-model-overpay`,category:`cost`,dataDeps:[`tokenData`,`liveConfig`],rule(e){let t=e.liveConfig?.settings?.model;if(Se(e.liveConfig?.settings)&&typeof t==`string`&&n(t)!==O)return null;let r=0,i=new Set;for(let t of e.tokenData)for(let e of t.entries){if(n(e.model)!==O)continue;let a=e.inputTokens/1e6*Fe;a>0&&(r+=a,i.add(t.sessionId))}return r<Ie?null:{id:`cost.legacy-model-overpay`,category:`cost`,severity:r>=1?`warning`:`info`,title:`Switch off legacy-priced Opus to current Opus`,detail:`${E(r)} of spend ran on claude-opus-4-1-* (legacy $15/MTok input) across ${i.size} session(s); the same capability tier on current Opus is $5/MTok — a ~67% overpay on those turns.`,action:`Update the pinned model string (settings.json or the SDK caller) from claude-opus-4-1-* to claude-opus-4-7 or newer.`,estSavingsUsd:r,affected:i.size,evidence:[...i].slice(0,5).map(e=>_(e)),view:`cost`,fix:{target:`settings.json`,label:`Pin current Opus`,note:`Set the model to a current-tier Opus in settings.json (or the SDK config that launches these runs). Suppressed once a non-legacy model is pinned.`,snippet:`{
  "model": "claude-opus-4-7"
}`}}}},Re=.5,ze=.1,Be={headings:[/^##\s+Web-search discipline\b/i],bodyPhrases:[`prefer web_fetch`]},Ve={id:`cost.web-search-spend`,category:`cost`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(w(e.liveConfig,Be))return null;let t=0,n=0,i=new Set;for(let r of e.tokenData){n+=s(r);for(let e of r.entries)e.webSearchRequests>0&&(t+=e.webSearchRequests,i.add(r.sessionId))}let a=t*r.webSearchRequest;if(a<Re||n<=0||a/n<=ze)return null;let o=Math.round(a/n*100);return{id:`cost.web-search-spend`,category:`cost`,severity:`info`,title:`Web-search requests are a material cost driver`,detail:`Web searches cost ~${E(a)} total ($0.01/request, ${t} requests across ${i.size} session(s)) — about ${o}% of estimated spend. Research loops often re-fetch the same queries each turn.`,action:`Prefer web_fetch for known stable URLs (no per-request charge), batch related lookups into one broad query, and cache fetched content in a file rather than re-searching.`,estSavingsUsd:a,affected:i.size,evidence:[...i].slice(0,5).map(e=>_(e)),view:`cost`,fix:{target:`CLAUDE.md`,label:`Add web-search discipline`,note:`Append to CLAUDE.md so research loops stop re-charging for repeated searches.`,snippet:`## Web-search discipline

- Prefer web_fetch for stable URLs (no per-request charge); web_search bills $0.01/request.
- Batch related lookups into one broad query instead of many narrow ones.
- Cache fetched content in a file — do not re-fetch the same URL across turns.`,appliedMarkers:Be}}}},He=3,Ue={id:`cost.priority-tier-spend`,category:`cost`,dataDeps:[`tokenData`],rule(e){let t=e.tokenData.filter(e=>e.serviceTier===`priority`);return t.length<He?null:{id:`cost.priority-tier-spend`,category:`cost`,severity:`info`,title:`Sessions ran on the priority service tier — confirm intent`,detail:`${t.length} session(s) used service_tier='priority' (a rate premium over standard); for routine automation the standard tier usually suffices.`,action:`Review which sessions/automation used the priority tier; ensure no SDK client or env var forces priority for non-latency-sensitive work.`,affected:t.length,evidence:t.slice(0,5).map(e=>_(e.sessionId)),view:`cost`}}},k=3;function We(e){let t=/^mcp__([^_]+(?:_[^_]+)*?)__/.exec(e);return t?t[1]:null}var Ge={id:`cost.idle-mcp-tools`,category:`cost`,dataDeps:[`toolInventories`],rule(e){let t=e.toolInventories;if(!t||t.length===0)return null;let n=te(t).unusedByFrequency.filter(e=>e.toolName.startsWith(`mcp__`)&&e.usedIn===0&&e.neverUsed>=k);if(n.length===0)return null;let r=Array.from(new Set(n.map(e=>We(e.toolName)).filter(e=>!!e)));return{id:`cost.idle-mcp-tools`,category:`cost`,severity:`info`,title:`Remove MCP tools loaded but never invoked`,detail:`${n.length} MCP tool(s) were loaded in ${k}+ sessions yet invoked zero times; each adds ~50-200 tokens to the tool manifest on every turn of every session that loads it.`,action:`Disable or remove the MCP server(s) backing the never-used tool(s) from ~/.claude.json (or scope them to the projects that need them).`,affected:n.length,evidence:n.slice(0,5).map(e=>`${e.toolName} — loaded in ${e.loadedIn}, used 0`),view:`tools`,fix:{target:`command`,label:`Disable idle MCP servers`,note:`Remove or scope these mcpServers in ~/.claude.json (or move them into a per-project .mcp.json). Servers with no invoked tools: ${r.join(`, `)||`(see evidence)`}.`,snippet:r.length?`# In ~/.claude.json, delete the "mcpServers" entries for:\n${r.map(e=>`#   - ${e}`).join(`
`)}`:`# In ~/.claude.json, delete the unused "mcpServers" entries (see the tool names above).`}}}},Ke=.1,qe=5,Je={id:`cost.expensive-agent-type`,category:`cost`,dataDeps:[`agentSettings`,`attribution`,`runtimeEvents`,`toolData`,`tokenData`],rule(e){if(S(e.liveConfig?.settings))return null;let t=l(e.agentSettings??[],e.attribution??[],e.runtimeEvents??[],e.toolData,e.tokenData).filter(e=>e.meanCostUsd>Ke&&e.runs>=qe);if(t.length===0)return null;t.sort((e,t)=>t.meanCostUsd-e.meanCostUsd);let n=t[0];return{id:`cost.expensive-agent-type`,category:`cost`,severity:`info`,title:`High mean cost per agent run`,detail:`Agent type "${n.agentType}" averages ${E(n.meanCostUsd)}/run over ${n.runs} runs${t.length>1?` (and ${t.length-1} other type(s) over $0.10/run)`:``}; right-sizing its model recovers most of that.`,action:`Pin a cheaper model (e.g. claude-haiku-4-5) for that agent type, narrow its task scope, or cap the context passed to it.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${e.agentType}: ${E(e.meanCostUsd)}/run × ${e.runs}`),view:`agents`,fix:{target:`settings.json`,label:`Pin a cheaper model for the agent`,note:`Set a cheaper model in the SDK config / settings.json that launches the flagged agent type.`,snippet:`{
  "model": "claude-haiku-4-5"
}`}}}},A={headings:[/^##\s+Context discipline\b/i],bodyPhrases:[`/compact`,`/clear`]},Ye={id:`context.over-window`,category:`context`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(w(e.liveConfig,A))return null;let t=ne(e.tokenData).filter(e=>e.peakContext>re);return t.length===0?null:{id:`context.over-window`,category:`context`,severity:`warning`,title:`Sessions spilled past the 200K context window`,detail:`${t.length} session(s) peaked above 200K tokens, where context is evicted and re-sent at cost.`,action:`Compact earlier or start a fresh session once a task is done — keep working context well under the window.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${_(e.sessionId)}, peak ${(e.peakContext/1e3).toFixed(0)}k`),view:`context`,fix:{target:`CLAUDE.md`,label:`Compact earlier`,note:`Append to your project (or ~/.claude) CLAUDE.md so Claude trims context before the window fills.`,snippet:`## Context discipline

Keep the working context well under the model's context window.
- When the conversation grows large (roughly 150K+ tokens) or right after finishing a discrete task, run \`/compact\` to summarise and reclaim space.
- When starting genuinely unrelated work, run \`/clear\` to begin a fresh session instead of carrying stale context.
- Avoid pulling large files or command output into context the current task doesn't need.`,appliedMarkers:A}}}},j={headings:[/^##\s+(Keep the session|Session) heal/i],bodyPhrases:[`cache reuse`,`switching permission modes`]},Xe={id:`context.low-health`,category:`context`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(w(e.liveConfig,j))return null;let t=ae(e.tokenData).filter(e=>e.score<50);return t.length===0?null:{id:`context.low-health`,category:`context`,severity:`warning`,title:`Low session-health scores`,detail:`${t.length} session(s) scored below 50 on the context-health heuristic (cache reuse, compaction, growth).`,action:`Open Context Health to see the per-session reasons and address the worst offenders.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${_(e.sessionId)}, ${e.score}, ${e.reasons[0]??``}`),view:`context`,fix:{target:`CLAUDE.md`,label:`Keep context healthy`,note:`Append to CLAUDE.md to curb the growth/churn that drags the health score down.`,snippet:`## Keep the session healthy

- Read a file once and work from that context; don't re-Read unchanged files.
- Don't load large or unrelated files "just in case" — pull only what the current task needs.
- After completing a task, \`/compact\` (to keep going) or \`/clear\` (for unrelated next work) rather than letting context grow unbounded.
- Avoid switching permission modes mid-session; it churns context and hurts cache reuse.`,appliedMarkers:j}}}},M={headings:[/^##\s+(Keep the )?prompt cache\b/i],bodyPhrases:[`stable context prefix`]},Ze={id:`context.low-cache-hit`,category:`context`,dataDeps:[`tokenData`,`liveConfig`],rule(e){if(w(e.liveConfig,M))return null;let t=ie(e.tokenData).filter(e=>e.totalReads+e.totalWrites>0);if(t.length===0)return null;let n=t.filter(e=>e.hitRate<p);if(n.length===0)return null;let r=t.reduce((e,t)=>e+t.hitRate,0)/t.length;return{id:`context.low-cache-hit`,category:`context`,severity:`info`,title:`Low cache hit rate on some sessions`,detail:`${n.length} session(s) read back less than ${(p*100).toFixed(0)}% of cached context (overall average ${(r*100).toFixed(0)}%). Low reuse means more tokens billed at full input rate.`,action:`Avoid frequent context churn within a session (large unrelated reads, mode switches) so the prompt cache stays warm.`,affected:n.length,view:`context`,fix:{target:`CLAUDE.md`,label:`Keep the cache warm`,note:`Append to CLAUDE.md so context stays stable within a session and the prompt cache is reused.`,snippet:`## Keep the prompt cache warm

Cache hits require a stable context prefix, so avoid churning it mid-session:
- Don't interleave large, unrelated file reads into focused work — batch related reads together.
- Avoid switching permission modes or models in the middle of a task.
- Don't re-Read files that haven't changed; reuse what's already in context.
- Group similar work so repeated context (same files, same instructions) is reused rather than re-sent at full input rate.`,appliedMarkers:M}}}},N=200,Qe=400,$e={id:`context.bloated-claude-md`,category:`context`,dataDeps:[`liveConfig`],rule(e){let t=C(e.liveConfig);if(t.length===0)return null;let n=t.split(`
`).length;return n<=N?null:{id:`context.bloated-claude-md`,category:`context`,severity:n>Qe?`critical`:`warning`,title:`Trim the oversized global CLAUDE.md`,detail:`Your merged global CLAUDE.md is ~${n} lines, over Anthropic's documented ${N}-line target; every session pays that token cost and instruction adherence degrades past it.`,action:"Move reference material into path-scoped .claude/rules/*.md (with a `paths:` frontmatter) or skills with disable-model-invocation, so rules load only when relevant files are touched.",affected:n,fix:{target:`CLAUDE.md`,label:`Split rules out of CLAUDE.md`,note:`Keep CLAUDE.md near the ~200-line target. Move topic-specific reference material into path-scoped rule files that load only when matching files are touched.`,snippet:`## Instructions size — split rules

Keep this file near Anthropic's ~200-line target. Past it, token cost rises every
session and instruction adherence degrades.

- Move topic-specific guidance into \`.claude/rules/<topic>.md\` with a \`paths:\`
  frontmatter so each rule loads only when a matching file is touched:

  \`\`\`markdown
  ---
  paths: ["src/payments/**"]
  ---
  # Payments conventions
  …
  \`\`\`

- Or move rarely-needed reference material into a skill with
  \`disable-model-invocation\` so it is read on demand, not every turn.`}}}},et=2,tt=40,P={headings:[/^##\s+Context discipline\b/i],bodyPhrases:[`/compact`,`/clear`]},nt={id:`context.compaction-hot-sessions`,category:`context`,dataDeps:[`tokenData`,`toolData`,`timelines`,`liveConfig`],rule(e){if(w(e.liveConfig,P))return null;let t=oe(m(e.tokenData,e.toolData,e.timelines??[]));if(t.hotSessions<et&&t.hotPercent<tt)return null;let n=t.topSuggestion?` Dominant fix for the cohort: ${t.topSuggestion}.`:``;return{id:`context.compaction-hot-sessions`,category:`context`,severity:`warning`,title:`Multiple sessions at high compaction risk`,detail:`${t.hotSessions} session(s) (${t.hotPercent.toFixed(0)}% of the fleet) are in the high compaction-risk band; uncontrolled compaction re-sends 10–40K tokens per event.`,action:`Run /compact at task boundaries, /clear when switching tasks, and scope Read with offset/limit.${n}`,affected:t.hotSessions,view:`context`,fix:{target:`CLAUDE.md`,label:`Add context discipline`,note:`Append to CLAUDE.md so sessions trim context before they thrash into repeated compaction.`,snippet:"## Context discipline\n\n- Run `/compact` at sub-task boundaries and `/clear` when switching tasks, rather than letting context grow until it auto-compacts.\n- Read with `offset`/`limit` and prefer Grep over reading whole large files.\n- Don't pull large or unrelated files into context the current task doesn't need.",appliedMarkers:P}}}},rt=2,F={headings:[/^##\s+Session resets\b/i],bodyPhrases:[`/clear`]},it={id:`context.repeated-compactions`,category:`context`,dataDeps:[`tokenData`,`toolData`,`timelines`,`liveConfig`],rule(e){if(w(e.liveConfig,F))return null;let t=m(e.tokenData,e.toolData,e.timelines??[]).filter(e=>e.compactionsObserved>=2);return t.length<rt?null:{id:`context.repeated-compactions`,category:`context`,severity:`info`,title:`Sessions compacting multiple times — chronic context pressure`,detail:`${t.length} session(s) compacted 2+ times; each carried compacted summary re-bills tokens on every subsequent turn.`,action:`After a compaction, /clear and start fresh when switching tasks rather than carrying the summary forward; split remaining work into a new session.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${_(e.sessionId)}, ${e.compactionsObserved} compactions`),view:`context`,fix:{target:`CLAUDE.md`,label:`Add session-reset guidance`,note:`Append to CLAUDE.md so a second compaction triggers a clean reset instead of carrying the summary forward.`,snippet:`## Session resets

- If a session compacts twice, run \`/clear\` and split the remaining work into a new session rather than carrying the compacted summary forward — it re-bills on every subsequent turn.`,appliedMarkers:F}}}},at=3,I={headings:[/^##\s+Tool output discipline\b/i],bodyPhrases:[`offset`,`limit`]},ot={id:`context.compaction-large-tool-outputs`,category:`context`,dataDeps:[`tokenData`,`toolData`,`timelines`,`liveConfig`],rule(e){if(w(e.liveConfig,I))return null;let t=m(e.tokenData,e.toolData,e.timelines??[]),n=t.filter(e=>e.riskClass!==`low`);if(n.length===0)return null;let r=n.reduce((e,t)=>e+t.largeToolOutputRate,0)/n.length,i=t.filter(e=>e.topFactor===`tool-output`).length;return r<.1||i<at?null:{id:`context.compaction-large-tool-outputs`,category:`context`,severity:`info`,title:`Large tool outputs are inflating context`,detail:`Across hot sessions the mean large-tool-output rate is ${Math.round(r*100)}% (over the ${Math.round(se*100)}% warn threshold), with tool output the dominant compaction factor on ${i} session(s).`,action:`Scope Read with offset/limit and prefer Grep over unfiltered cat so tool outputs stay under the large-output threshold.`,affected:i,view:`context`,fix:{target:`CLAUDE.md`,label:`Add tool-output discipline`,note:`Append to CLAUDE.md so large tool outputs stop dominating context growth.`,snippet:"## Tool output discipline\n\n- Read with `offset`/`limit` instead of whole large files; prefer Grep/Glob over unfiltered `cat` so a single tool result stays small.\n- Don't echo large command output into context the current task doesn't need.",appliedMarkers:I}}}},st={id:`workflow.native-bypass`,category:`workflow`,dataDeps:[`toolData`,`liveConfig`],rule(e){let t=d(e.toolData);if(t.totalBypass<10)return null;let n=t.categories.slice(0,4),r={grep:`Bash(grep:*)`,find:`Bash(find:*)`,cat:`Bash(cat:*)`,sed:`Bash(sed:*)`,awk:`Bash(awk:*)`},i=n.map(e=>r[e.category]).filter(e=>!!e),a=i.length>0?i:[`Bash(grep:*)`,`Bash(find:*)`,`Bash(cat:*)`];return v(e.liveConfig?.settings,`deny`,a)||v(e.liveConfig?.settings,`ask`,a)?null:{id:`workflow.native-bypass`,category:`workflow`,severity:`info`,title:`Use native tools instead of shell equivalents`,detail:`${t.totalBypass} Bash commands re-implemented a native tool: ${n.map(e=>`${e.category} (${e.count})`).join(`, `)}.`,action:`Prefer Grep/Glob/Read/Edit over grep/find/cat/sed — they are faster, cheaper, and integrate with permissions.`,affected:t.totalBypass,evidence:n.map(e=>`${e.category} → ${e.nativeTool}: ${e.count}×`),view:`tools`,fix:{target:`settings.json`,label:`Add deny rules`,note:`Merge into the "permissions" object in .claude/settings.json (deep-merge the "deny" array). Use "ask" instead of "deny" if you want a prompt rather than a hard block — both steer Claude onto native Grep/Glob/Read.`,snippet:JSON.stringify({permissions:{deny:a}},null,2)}}}},L={headings:[/^##\s+Key files\b/i],bodyPhrases:[`kept in context`]},ct={id:`workflow.redundant-reads`,category:`workflow`,dataDeps:[`toolData`,`tokenData`,`liveConfig`],rule(e){if(w(e.liveConfig,L))return null;let t=le(e.toolData,e.tokenData);if(t.length===0)return null;let n=t.filter(e=>e.compactions>0).length;return{id:`workflow.redundant-reads`,category:`workflow`,severity:`info`,title:`Files re-read repeatedly within a session`,detail:`${t.length} (session, file) pair(s) re-Read the same file 3+ times${n>0?`, ${n} alongside compaction (eviction-driven)`:``}. Each re-read re-pays the token cost.`,action:`Pin frequently-needed files in CLAUDE.md or pass their contents once instead of re-reading.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${_(e.sessionId)}, ${D(e.filePath)}, ${e.reads}×`),view:`files`,fix:{target:`CLAUDE.md`,label:`Pin files in CLAUDE.md`,note:`Append to your project CLAUDE.md. The @-prefix imports the file so its contents load once into context instead of being re-Read each time. Replace the basenames with repo-relative paths.`,snippet:`## Key files (kept in context)\n\n${Array.from(new Set(t.map(e=>D(e.filePath)))).slice(0,5).map(e=>`- @${e}`).join(`
`)}`,appliedMarkers:L}}}},R={headings:[/^##\s+Common commands\b/i],bodyPhrases:[`wrap them in a script`]},lt={id:`workflow.repeated-commands`,category:`workflow`,dataDeps:[`toolData`,`liveConfig`],rule(e){if(w(e.liveConfig,R))return null;let t=u(e.toolData);return t.length===0?null:{id:`workflow.repeated-commands`,category:`workflow`,severity:`info`,title:`Repeated commands are candidates for automation`,detail:`${t.length} command(s) ran 3+ times within a session — repetitive manual steps that a hook, skill, or Makefile target could absorb.`,action:`Wrap the most-repeated commands in a script or a SessionStart/PostToolUse hook.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${e.totalCount}×, ${Te(e.command,70)}`),view:`tools`,fix:{target:`CLAUDE.md`,label:`Document a wrapper`,note:`Append to your project CLAUDE.md. Wrap these in a script or Make target and point the entry here, so future sessions run one command instead of repeating the steps.`,snippet:`## Common commands

These were run repeatedly — wrap them in a script (e.g. \`scripts/dev.sh\`) or a Makefile target and invoke that instead of re-typing the steps:

`+t.slice(0,3).map(e=>`- \`${Te(e.command,70)}\``).join(`
`),appliedMarkers:R}}}},ut={id:`workflow.file-churn`,category:`workflow`,dataDeps:[`toolData`],rule(e){let t=ce(e.toolData).filter(e=>e.churn>=15);return t.length===0?null:{id:`workflow.file-churn`,category:`workflow`,severity:`info`,title:`High-churn files`,detail:`${t.length} file(s) were edited/written 15+ times. Heavy churn can signal an unclear interface or repeated trial-and-error.`,action:`Review whether these files need refactoring or clearer up-front specs to reduce rework.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${D(e.filePath)}, ${e.churn} edits / ${e.sessions} sessions`),view:`files`}}},dt={id:`workflow.assistant-refusal-rate`,category:`workflow`,dataDeps:[`assistantFeatures`],rule(e){let t=e.assistantFeatures??[];if(t.length===0)return null;let n=0,r=0;for(let e of t)n+=e.assistantTurnCount,r+=e.refusalCount;if(n<50)return null;let i=r/n;return i<.15?null:{id:`workflow.assistant-refusal-rate`,category:`workflow`,severity:`info`,title:`Assistant frequently course-corrects`,detail:`${Math.round(i*100)}% of assistant turns (${r} of ${n}) contain a refusal or concession ("I cannot", "I apologize", "you're right") — often a sign the prompt or project context left it guessing.`,action:`Tighten recurring instructions in CLAUDE.md (constraints, file locations, conventions) so the assistant needs fewer corrections.`,affected:r}}},z=30,B=1440*60*1e3;function ft(e,t){let n=new Map(t.map(e=>[e.sessionId,e.startTime])),r=[];for(let t of e){let e=n.get(t.sessionId);e!=null&&r.push({startTime:e,agents:V(t.agents),skills:V(t.skills),mcpServers:V(t.mcpServers)})}return r}function V(e){let t={};for(let[n,r]of Object.entries(e))r&&typeof r.invocations==`number`&&(t[n]=r.invocations);return t}function H(e,t,n,r){let i=null,a=0,o=0;for(let s of t){let t=r(s,e);t<=0||(a+=t,s.startTime>=n&&(o+=t),(i==null||s.startTime>i)&&(i=s.startTime))}return{lastSeen:i,lifetimeCount:a,windowCount:o}}function pt(e,t){if(e.length===0)return 1/0;let n=t;for(let t of e)t.startTime>0&&t.startTime<n&&(n=t.startTime);return Math.max(0,(t-n)/B)}function U(e){let{resourceType:t,resourceId:n,scope:r,summary:i,hedge:a}=e;return{id:`${t}.unused:${n}${r.kind===`project`?`@${r.project}`:``}`,resourceType:t,resourceId:n,scope:r,state:`unused`,lastSeen:i.lastSeen,lifetimeCount:i.lifetimeCount,windowCount:i.windowCount,windowDays:z,...a?{hedge:a}:{}}}function mt(e,t,n,r){let i=[];for(let a of e){let e=H(a.id,t,n,(e,t)=>e.mcpServers[t]??0);if(e.windowCount>0)continue;let o=a.scope===`global`?{kind:`global`}:{kind:`project`,project:a.enabledByProjects?.[0]??`(unknown)`};i.push(U({resourceType:`mcpServer`,resourceId:a.id,scope:o,summary:e,hedge:r}))}return i}function ht(e,t,n,r){let i=[];for(let a of e){let e=a.bundled;if(!e)continue;let o=e.skills??[],s=e.agents??[];if(o.length===0&&s.length===0)continue;let c=null,l=0,u=0;for(let e of o){let r=H(e,t,n,(e,t)=>e.skills[t]??0);l+=r.lifetimeCount,u+=r.windowCount,r.lastSeen!=null&&(c==null||r.lastSeen>c)&&(c=r.lastSeen)}for(let e of s){let r=H(e,t,n,(e,t)=>e.agents[t]??0);l+=r.lifetimeCount,u+=r.windowCount,r.lastSeen!=null&&(c==null||r.lastSeen>c)&&(c=r.lastSeen)}u>0||i.push(U({resourceType:`plugin`,resourceId:a.id,scope:a.scope===`project`?{kind:`project`,project:a.installPath||`(unknown)`}:{kind:`global`},summary:{lastSeen:c,lifetimeCount:l,windowCount:u},hedge:r}))}return i}function W(e){let t=e.liveConfig;if(!t)return[];let n=e.now??Date.now(),r=n-z*B,i=pt(e.sessions,n)<z?`window-shorter-than-threshold`:void 0,a=ft(e.attribution,e.sessions),o=[];for(let e of t.skills){let t=H(e.id,a,r,(e,t)=>e.skills[t]??0);t.windowCount>0||o.push(U({resourceType:`skill`,resourceId:e.id,scope:{kind:`global`},summary:t,hedge:i}))}for(let e of t.subagents){let t=H(e.id,a,r,(e,t)=>e.agents[t]??0);t.windowCount>0||o.push(U({resourceType:`subagent`,resourceId:e.id,scope:{kind:`global`},summary:t,hedge:i}))}return o.push(...mt(t.mcpServers,a,r,i)),o.push(...ht(t.plugins,a,r,i)),_t(o)}var gt={skill:0,subagent:1,mcpServer:2,plugin:3};function _t(e){return e.slice().sort((e,t)=>{let n=gt[e.resourceType]-gt[t.resourceType];if(n!==0)return n;let r=e.lastSeen??-1,i=t.lastSeen??-1;return r===i?e.resourceId.localeCompare(t.resourceId):r-i})}var vt={skill:`Skills`,subagent:`Subagents`,mcpServer:`MCP servers`,plugin:`Plugins`},yt=3,bt={id:`workflow.unused-installed-skills`,category:`workflow`,dataDeps:[`liveConfig`,`attribution`,`sessions`],rule(e,t){if(!e.liveConfig)return null;let n=W({liveConfig:e.liveConfig,attribution:e.attribution??[],sessions:e.sessions.map(e=>({sessionId:e.sessionId,startTime:e.startTime})),now:t}).filter(e=>e.resourceType===`skill`&&e.windowCount===0);return n.length<yt?null:{id:`workflow.unused-installed-skills`,category:`workflow`,severity:`info`,title:`Installed skills unused in the last 30 days`,detail:`${n.length} installed skill(s) had zero invocations in the last 30 days; unused skills add selection ambiguity when Claude picks which to invoke.`,action:`Remove unused skills from ~/.claude/skills/ to keep skill-selection clean.`,affected:n.length,evidence:n.slice(0,5).map(e=>e.resourceId)}}},xt=new Set([`Edit`,`Write`,`MultiEdit`,`NotebookEdit`]),St=10,Ct=.1;function wt(e){let t=e?.hooks?.PreToolUse;return Array.isArray(t)?t.some(e=>/\bEdit\b|\bWrite\b/.test(typeof e?.matcher==`string`?e.matcher:``)):!1}var Tt={id:`workflow.tool-undo-rate`,category:`workflow`,dataDeps:[`toolData`,`apiErrors`,`timelines`,`liveConfig`],rule(e){if(wt(e.liveConfig?.settings))return null;let t=f(e.toolData,e.apiErrors,e.timelines??[]).filter(e=>xt.has(e.tool)&&e.invocations>=St&&e.immediatelyFollowedByUndo/e.invocations>=Ct);if(t.length===0)return null;let n=e=>e.immediatelyFollowedByUndo/e.invocations;t.sort((e,t)=>n(t)-n(e));let r=t[0];return{id:`workflow.tool-undo-rate`,category:`workflow`,severity:`info`,title:`Reduce immediate rollbacks on file-editing tools`,detail:`${r.tool} edits are rolled back (git checkout/restore/revert immediately after) ${Math.round(n(r)*100)}% of the time over ${r.invocations} invocations — each undo cycle is 2+ wasted round-trips.`,action:`Add a PreToolUse hook that runs a quick lint/typecheck before edits commit, catching errors before they need a rollback.`,affected:r.immediatelyFollowedByUndo,evidence:t.slice(0,5).map(e=>`${e.tool}: ${Math.round(n(e)*100)}% undo over ${e.invocations}`),view:`tools`,fix:{target:`settings.json`,label:`Add a pre-edit check hook`,note:`Merge into .claude/settings.json hooks. Swap the command for your real lint/typecheck so broken edits are caught before they need rolling back.`,snippet:`{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "npm run -s lint" }
        ]
      }
    ]
  }
}`}}}},Et=10,G=.4,Dt={id:`workflow.low-tool-effectiveness`,category:`workflow`,dataDeps:[`toolData`,`apiErrors`,`timelines`,`liveConfig`],rule(e){if(x(e.liveConfig?.settings))return null;let t=f(e.toolData,e.apiErrors,e.timelines??[]).filter(e=>e.effectivenessScore<G&&e.invocations>=Et);if(t.length===0)return null;t.sort((e,t)=>e.effectivenessScore-t.effectivenessScore);let n=t[0];return{id:`workflow.low-tool-effectiveness`,category:`workflow`,severity:`info`,title:`Fix tools with persistently low effectiveness`,detail:`Tool ${n.tool} has an effectiveness score of ${n.effectivenessScore.toFixed(2)} (below ${G}) over ${n.invocations} invocations — error/undo/retry signals outweigh forward motion.`,action:`Add a PostToolUse hook that validates the result (exit code, path-exists, typecheck) for the flagged tool so errors surface before the next turn.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${e.tool}: score ${e.effectivenessScore.toFixed(2)} over ${e.invocations}`),view:`tools`,fix:{target:`hook`,label:`Validate the low-scoring tool`,note:`Merge into .claude/settings.json hooks. Validates ${n.tool} results so errors surface immediately; swap the command for your real check.`,snippet:`{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "${n.tool}",
        "hooks": [
          { "type": "command", "command": "npm run -s typecheck" }
        ]
      }
    ]
  }
}`}}}};function K(e){return e.tokenDeltaCount>0?e.tokenDeltaSum/e.tokenDeltaCount:null}function q(e){return e.costDeltaCount>0?e.costDeltaSum/e.costDeltaCount:null}function Ot(e){let t=q(e);if(t!==null)return t<0;let n=K(e);return n===null?null:n<0}var kt=5,At=3,jt=.6,Mt=3,Nt={model:{label:`cheaper model`,adopt:`default to the cheaper model tier for this class of task`},reasoning:{label:`different reasoning effort`,adopt:`adjust extended-thinking depth to match what the experiments favoured`},prompt:{label:`structured prompt`,adopt:`frame these tasks with explicit steps / acceptance criteria up front`},skills:{label:`a dedicated skill`,adopt:`route this kind of task through the matching skill (e.g. /diagnose, /tdd)`},plugins:{label:`a plugin workflow`,adopt:`run these through the plugin workflow instead of ad-hoc steps`},mcp:{label:`an MCP server`,adopt:`prefer the MCP path over the native one for these tasks`},subagents:{label:`subagent fan-out`,adopt:`parallelise discovery with subagents / Explore for these tasks`},tools:{label:`a different tool strategy`,adopt:`switch the default tool strategy for these tasks`},mode:{label:`plan-first mode`,adopt:`plan before acting on these tasks`},context:{label:`fresh context`,adopt:`attempt these from a clean context rather than carrying full session state`}};function J(e){return Nt[e]??{label:`the "${e}" variation`,adopt:`adopt the "${e}" variation as the default`}}function Pt(e){return e.shadowWins+e.mainWins}var Ft={id:`workflow.shadow-axis-wins`,category:`workflow`,dataDeps:[`shadowCalls`],rule(e){let t=e.shadowCalls;if(!t||t.total===0||t.byAxis.length===0)return null;let n=[];for(let e of t.byAxis){if(e.samples<kt)continue;let t=Pt(e);if(t<At)continue;let r=e.shadowWins/t;if(r<jt)continue;let i=Ot(e)===!0,a=q(e),o=K(e),s=e.shadowWins+e.liveShadowWins*2+(i?2:0);n.push({a:e,winRate:r,cheaper:i,costDelta:a,tokenDelta:o,score:s})}if(n.length===0)return null;n.sort((e,t)=>t.score-e.score);let r=n[0],i=r.a,a=J(i.axis),o=i.liveShadowWins>=Mt,s=o?`warning`:`info`,c=Math.round(r.winRate*100),l=`${i.live} live + ${i.replay} replay`,u=r.cheaper?r.costDelta===null?` and used ~${Math.abs(Math.round(r.tokenDelta))} fewer tokens on average`:` and cost ~$${Math.abs(r.costDelta).toFixed(2)} less on average`:``,d=o?`${i.liveShadowWins} of these were confirmed in live, in-the-loop runs`:`evidence is mostly from replay (cold-start caveat), so treat this as a lead to confirm live`,f=n.slice(1,3).map(e=>`${J(e.a.axis).label} (${Math.round(e.winRate*100)}% over ${e.a.samples})`);return{id:`workflow.shadow-axis-wins`,category:`workflow`,severity:s,title:`Adopt ${a.label} — it out-performed your default in shadow tests`,detail:`Across ${i.samples} shadow experiment(s) on the "${i.axis}" axis (${l}), the variation won ${i.shadowWins}/${Pt(i)} decided comparisons (${c}%)${u}. ${d}.`,action:`For this class of task, ${a.adopt}. Re-run a few live shadows to confirm before making it your standing default.`,affected:i.samples,view:`recommendations`,evidence:[`${i.axis}: shadow ${i.shadowWins} / main ${i.mainWins} / tie ${i.ties} over ${i.samples} (${l})`,r.costDelta===null?r.tokenDelta===null?`no paired cost/token data`:`avg token delta (shadow−main): ${Math.round(r.tokenDelta)}`:`avg $ delta (shadow−main): $${r.costDelta.toFixed(2)}`,...f.length?[`other promising axes: ${f.join(`, `)}`]:[]],fix:{target:`CLAUDE.md`,label:`Adopt the winning variation`,note:`Shadow-calls evidence (epic #513). Add a standing note so this becomes the default for this kind of task; keep shadowing to catch regressions.`,snippet:`## Default approach (from shadow-calls #513)\n\nFor this class of task, ${a.adopt} — shadow experiments on the "${i.axis}" axis won ${c}% of ${i.samples} comparisons${r.cheaper?` at lower token cost`:``}. Revisit if live shadows stop favouring it.`,appliedMarkers:{headings:[/^##\s+default approach/i],bodyPhrases:[`shadow-calls`]}}}}},It=5,Lt=3,Rt=.6,zt={model:[`cost.legacy-model-overpay`,`cost.unknown-model`],tools:[`workflow.native-bypass`],context:[`context.over-window`,`context.repeated-compactions`],reasoning:[],prompt:[],skills:[],plugins:[],mcp:[],subagents:[],mode:[]},Bt={reasoning:`context`,prompt:`workflow`,skills:`workflow`,plugins:`workflow`,mcp:`workflow`,subagents:`workflow`,mode:`workflow`};function Y(e){return e.shadowWins+e.mainWins}var Vt={id:`workflow.uncovered-shadow-axis`,category:`workflow`,dataDeps:[`shadowCalls`],rule(e){let t=e.shadowCalls;if(!t||t.total===0)return null;let n=t.byAxis.filter(e=>{if(e.samples<It)return!1;let t=Y(e);if(t<Lt||e.shadowWins/t<Rt)return!1;let n=zt[e.axis];return n!==void 0&&n.length===0}).sort((e,t)=>t.shadowWins-e.shadowWins);if(n.length===0)return null;let r=n[0],i=Y(r),a=Math.round(r.shadowWins/i*100),o=`${Bt[r.axis]??`workflow`}.shadow-${r.axis}`,s=q(r),c=K(r),l=n.map(e=>e.axis).join(`, `),u=`New recommendation class to add: "${r.axis}" wins in shadow tests but no rule covers it`,d=`Shadow-calls (#513) show the "${r.axis}" variation winning ${r.shadowWins}/${i} (${a}%) over ${r.samples} experiments, but no detector encodes that pattern. Propose a dedicated detector \\\`${o}\\\`.`;return{id:`workflow.uncovered-shadow-axis`,category:`workflow`,severity:`info`,title:u,detail:`Shadow experiments favour the "${r.axis}" axis (${r.shadowWins}/${i} = ${a}% over ${r.samples}), but the recommendation catalog has no rule for it`+(s!==null&&s<0?`, and it cost ~$${Math.abs(s).toFixed(2)} less on average`:c!==null&&c<0?`, and it ran cheaper (~${Math.abs(Math.round(c))} fewer tokens avg)`:``)+`. That's a candidate for a new recommendation class. Uncovered axes with wins: ${l}.`,action:`Propose a dedicated detector (e.g. \`${o}\`) so this pattern becomes a permanent, verifiable recommendation rather than living only in the ledger. File it as a backlog issue (per the engine-gap feedback loop).`,affected:n.length,view:`recommendations`,evidence:n.map(e=>`${e.axis}: shadow ${e.shadowWins}/${Y(e)} (${e.live}L+${e.replay}R), no covering rule → suggest ${Bt[e.axis]??`workflow`}.shadow-${e.axis}`),fix:{target:`command`,label:`File a new-rule proposal`,note:`Discovery proposal (epic #513 / ADR 0002). Dedup against open issues titled "[shadow-discovery]" before filing.`,snippet:`gh issue create --repo shpwrck/claude-history-dashboard --label backlog --label enhancement \\\n  --title "[shadow-discovery] New detector for winning axis: ${r.axis}" \\\n  --body "${d}"`}}}},Ht=1440*60*1e3,Ut={id:`workflow.abandoned-tasks`,category:`workflow`,dataDeps:[`tasks`],rule(e,t){let n=e.tasks??[];if(!n.length)return null;let r=7*Ht,i=e=>e.status===`pending`||e.status===`in_progress`,a=new Map;for(let e of n){let t=a.get(e.sessionId)??[];t.push(e),a.set(e.sessionId,t)}let o=[];for(let[e,n]of a){let a=n.filter(i);if(!a.length)continue;let s=t-Math.max(...n.map(e=>e.mtimeMs));if(s<r)continue;let c=Math.round(s/Ht);o.push({sessionId:e,daysSinceActive:c,count:a.length,subjects:a.map(e=>e.subject)})}if(!o.length)return null;o.sort((e,t)=>t.count-e.count);let s=o.reduce((e,t)=>e+t.count,0),c=o[0],l=o.slice(0,5).map(e=>`${_(e.sessionId)}: ${e.count} open task(s), ${e.daysSinceActive}d idle`),u=o.slice(0,3).map(e=>`cat ~/.claude/tasks/${e.sessionId}/*.json | jq 'select(.status!="completed") | .subject'`).join(`
`);return{id:`workflow.abandoned-tasks`,category:`workflow`,severity:`warning`,title:`Abandoned open tasks in cold sessions`,detail:`${s} open task(s) across ${o.length} session(s) idle >= 7 days — work that was never PR'd or formally dropped. Worst: ${_(c.sessionId)} with ${c.count} task(s) idle ${c.daysSinceActive}d.`,action:`Re-open, reassign, or formally close these tasks. Use the fix snippet to inspect what is still open per session.`,affected:s,evidence:l,fix:{target:`command`,label:`Inspect abandoned tasks`,note:`Run per-session to list tasks never completed. Close or reassign each one.`,snippet:u}}}},Wt={id:`workflow.blocked-task-pileup`,category:`workflow`,dataDeps:[`tasks`],rule(e){let t=e.tasks??[];if(!t.length)return null;let n=[],r=new Map;for(let e of t){let t=r.get(e.sessionId)??[];t.push(e),r.set(e.sessionId,t)}for(let[e,t]of r){let r=new Map(t.map(e=>[e.id,e])),i=new Map;for(let e of t)if(e.status!==`completed`)for(let t of e.blockedBy){let n=r.get(t);if(!n||n.status===`completed`)continue;let a=i.get(t)??[];a.push(e),i.set(t,a)}for(let[t,a]of i){if(a.length<2)continue;let i=r.get(t);n.push({sessionId:e,rootId:t,rootSubject:i.subject,blockedCount:a.length,blockedSubjects:a.map(e=>e.subject)})}}if(!n.length)return null;n.sort((e,t)=>t.blockedCount-e.blockedCount);let i=n[0],a=n.reduce((e,t)=>e+t.blockedCount,0),o=n.slice(0,5).map(e=>`${_(e.sessionId)}: ${e.blockedCount} task(s) blocked behind "${e.rootSubject}"`),s=n.slice(0,3).map(e=>`# Unblock: "${e.rootSubject}" (session ${_(e.sessionId)})\n# Assign an owner or split the root; ${e.blockedCount} downstream task(s) then unblock:\n`+e.blockedSubjects.map(e=>`#   - ${e}`).join(`
`)).join(`

`);return{id:`workflow.blocked-task-pileup`,category:`workflow`,severity:`warning`,title:`Blocked task pileup — unfinished DAG root holding up downstream work`,detail:`${a} open task(s) across ${n.length} pileup(s) are stalled behind an unfinished root. Worst: ${i.blockedCount} task(s) waiting on "${i.rootSubject}" in session ${_(i.sessionId)}.`,action:`Assign an owner to the root task first, or split it into smaller steps. Until the root is resolved, the blocked tasks are dead weight.`,affected:a,evidence:o,fix:{target:`command`,label:`Identify and unblock root tasks`,note:`Review each root task. Assign an owner or split it; downstream tasks unblock automatically.`,snippet:s}}}},Gt=3,Kt=10,qt={id:`workflow.rework-signature`,category:`workflow`,dataDeps:[`sessions`],rule(e){let t=e.fileHistory??[];if(t.length<Gt)return null;let n=[...t].sort((e,t)=>t.reworkScore-e.reworkScore)[0];if(n.reworkScore<Kt)return null;let r=[...t].sort((e,t)=>t.reworkScore-e.reworkScore).slice(0,5);return{id:`workflow.rework-signature`,category:`workflow`,severity:n.burstRate>=3?`warning`:`info`,title:`High file-snapshot churn signals retry storms`,detail:`Session ${_(n.sessionId)} has reworkScore ${n.reworkScore} (${n.churn} pre-edit checkpoints, ${n.burstRate}/min burst rate). High burst rates indicate rapid successive file edits — a retry-storm pattern that wastes round-trips and signals unclear prompts or brittle edits.`,action:`Review high-rework sessions: add clearer up-front specs or pre-edit hooks (lint/typecheck) to catch errors before they cascade into repeated checkpoint cycles.`,affected:r.length,evidence:r.map(e=>`${_(e.sessionId)}: score=${e.reworkScore}  churn=${e.churn}  burst=${e.burstRate}/min  span=${e.spanMin}m`),view:`files`}}},X=6,Z=1e3,Jt={id:`workflow.plan-missing-verification`,category:`workflow`,dataDeps:[],rule(e){let t=e.plans??[];if(t.length===0)return null;let n=t.filter(e=>!e.hasVerification&&(e.fileRefs>=X||e.words>=Z));if(n.length===0)return null;let r=t.length,i=Math.round(n.length/r*100),a=n.slice(0,5).map(e=>e.name);return{id:`workflow.plan-missing-verification`,category:`workflow`,severity:n.length>=3?`warning`:`info`,title:`Large plans are missing a Verification section`,detail:`${n.length} of ${r} plan(s) (${i}%) exceed the complexity threshold (${X}+ file refs or ${Z}+ words) and carry no ## Verification or ## Test section — the plans most likely to ship under-specified.`,action:`Add a ## Verification section to each large plan before running it. Describe the minimal observable signal that proves the plan succeeded.`,affected:n.length,evidence:a,fix:{target:`CLAUDE.md`,label:`Add plan Verification rule`,note:`Paste this into your CLAUDE.md to enforce a Verification section on large plans.`,snippet:`## Plan Verification discipline\n\nBefore executing any plan that touches ${X}+ files or exceeds ${Z} words, it MUST include a ## Verification section describing the minimal observable signal that proves the plan succeeded. No Verification section = plan is not ready to run.`,appliedMarkers:{headings:[/^##\s+plan verification/i],bodyPhrases:[`verification section`,`plan is not ready to run`]}}}}},Yt={id:`safety.dangerous-bypass`,category:`safety`,dataDeps:[`toolData`,`tokenData`,`permissionRows`,`liveConfig`],rule(e){let t=ue(e.toolData);if(t.length===0||v(e.liveConfig?.settings,`deny`,Ce))return null;let n=new Set(e.tokenData.filter(e=>o(e.entrypoint)).map(e=>e.sessionId)),r=e=>e.some(e=>n.has(e.sessionId)),i=fe(t,e.permissionRows).filter(e=>e.bypassMode&&e.dangerousCount>0);if(i.length>0){let e=i.reduce((e,t)=>e+t.dangerousCount,0),n=new Set(i.map(e=>e.sessionId)),a=r(t.filter(e=>n.has(e.sessionId))),o=`critical`;return{id:`safety.dangerous-bypass`,category:`safety`,severity:a?g(o):o,unattended:a,title:`Dangerous commands ran under bypassed permissions`,detail:`${e} risky command(s) (e.g. rm -rf, git reset --hard, curl|sh) ran in ${i.length} session(s) that used bypassPermissions.`,action:`Reserve bypassPermissions for trusted, reversible work; add an explicit deny-list for destructive patterns.`,affected:e,evidence:t.slice(0,5).map(e=>`${_(e.sessionId)}, ${e.pattern}`),view:`permissions`,fix:{target:`settings.json`,label:`Add destructive-command deny rules`,note:`Merge into the "permissions" object in .claude/settings.json (deep-merge "deny"), and reserve bypassPermissions for trusted, reversible work. curl|sh cannot be prefix-matched, so curl/wget are denied wholesale — drop them if too broad.`,snippet:`{
  "permissions": {
    "deny": [
      "Bash(rm -rf:*)",
      "Bash(rm -fr:*)",
      "Bash(git reset --hard:*)",
      "Bash(git clean -fd:*)",
      "Bash(git push --force:*)",
      "Bash(git push -f:*)",
      "Bash(dd:*)",
      "Bash(mkfs:*)",
      "Bash(shred:*)",
      "Bash(curl:*)",
      "Bash(wget:*)"
    ]
  }
}`}}}if(v(e.liveConfig?.settings,`ask`,we))return null;let a=r(t),s=`warning`;return{id:`safety.dangerous-commands`,category:`safety`,severity:a?g(s):s,unattended:a,title:`Dangerous command patterns detected`,detail:`${t.length} command(s) matched a destructive pattern (rm -rf, git push --force, dd, …).`,action:`Spot-check these were intentional; consider a hook that confirms before destructive ops.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${_(e.sessionId)}, ${e.pattern}`),view:`permissions`,fix:{target:`settings.json`,label:`Confirm before destructive commands`,note:`Merge into the "permissions" object in .claude/settings.json (deep-merge "ask"). "ask" forces an interactive confirmation rather than blocking outright.`,snippet:`{
  "permissions": {
    "ask": [
      "Bash(rm -rf:*)",
      "Bash(rm -fr:*)",
      "Bash(git reset --hard:*)",
      "Bash(git clean -fd:*)",
      "Bash(git push --force:*)",
      "Bash(git push -f:*)",
      "Bash(dd:*)",
      "Bash(mkfs:*)",
      "Bash(shred:*)"
    ]
  }
}`}}}},Xt={id:`safety.prompt-friction`,category:`safety`,dataDeps:[`toolData`,`permissionRows`,`liveConfig`],rule(e){let t=de(e.toolData,e.permissionRows),n=t[0];if(!n||n.promptableCalls<20||n.share<25)return null;let r=n.toolName===`Bash`?T:[n.toolName];return v(e.liveConfig?.settings,`allow`,r)?null:{id:`safety.prompt-friction`,category:`safety`,severity:`info`,title:`Reduce permission-prompt friction`,detail:`${n.toolName} accounts for ${n.share.toFixed(0)}% of prompt-eligible tool calls (${n.promptableCalls} across ${n.sessionCount} sessions).`,action:`Seed a project allowlist for the safe variants of this tool — the /fewer-permission-prompts skill can do this from your transcripts.`,affected:n.promptableCalls,evidence:t.slice(0,4).map(e=>`${e.toolName}: ${e.promptableCalls} (${e.share.toFixed(0)}%)`),view:`permissions`,fix:{target:`settings.json`,label:`Allowlist safe ${n.toolName} calls`,note:`Merge into .claude/settings.json — deep-merge the "permissions.allow" array. Allowed calls stop prompting; the Bash list below is limited to read-only/idempotent commands.`,snippet:n.toolName===`Bash`?JSON.stringify({permissions:{allow:T}},null,2):JSON.stringify({permissions:{allow:[n.toolName]}},null,2)}}}},Zt={id:`safety.deny-rule-never-triggered`,category:`safety`,dataDeps:[`toolData`,`liveConfig`],rule(e){let t=e.liveConfig?.settings?.permissions?.deny;if(!Array.isArray(t)||t.length===0)return null;let n=[];for(let r of t){if(typeof r!=`string`)continue;let{tool:t,specifier:i}=y(r);if(t===`Bash`&&i!==null){if(xe(r))continue}else if(i!==null)continue;let a=!1;for(let t of e.toolData){for(let e of t.calls)if(ve(r,e)===!0){a=!0;break}if(a)break}a||n.push(r)}return n.length===0?null:{id:`safety.deny-rule-never-triggered`,category:`safety`,severity:`info`,title:`Unused permission deny rules`,detail:`${n.length} permission deny rule(s) never matched a command attempt in your retained history — they may be leftover clutter rather than active guards.`,action:`Review whether these are still needed; pruning dead entries keeps settings.json honest. Known dangerous-command guards are deliberately excluded from this check, so anything listed is a non-destructive rule that simply never fired.`,evidence:n,affected:n.length,view:`permissions`,fix:{target:`settings.json`,label:`Prune unused deny rules`,note:`Remove these never-matched entries from the "permissions.deny" array in settings.json if they are no longer relevant. (Destructive-command guards are never listed here.)`,snippet:JSON.stringify({permissions:{deny:n}},null,2)}}}},Qt={id:`safety.allow-rule-overlaps-deny`,category:`safety`,dataDeps:[`liveConfig`],rule(e){let t=e.liveConfig?.settings?.permissions,n=Array.isArray(t?.allow)?t.allow:[],r=Array.isArray(t?.deny)?t.deny:[];if(n.length===0||r.length===0)return null;let i=[];for(let e of n)if(typeof e==`string`){for(let t of r)if(typeof t==`string`&&ye(e,t)){i.push({allow:e,deny:t});break}}return i.length===0?null:{id:`safety.allow-rule-overlaps-deny`,category:`safety`,severity:`warning`,title:`Allow rules shadowed by a deny`,detail:`${i.length} permission allow rule(s) are fully covered by a deny rule — deny always wins, so the allow never takes effect.`,action:`Remove the dead allow entries (or narrow the deny if the allow was the intent) so settings.json reflects what actually happens.`,evidence:i.map(e=>`${e.allow}  ⊂  deny ${e.deny}`),affected:i.length,view:`permissions`,fix:{target:`settings.json`,label:`Remove shadowed allow rules`,note:`These "permissions.allow" entries are overridden by a broader "deny" (deny wins), so they do nothing. Delete them, or narrow the conflicting deny if the allow was intended.`,snippet:JSON.stringify({permissions:{allow:i.map(e=>e.allow)}},null,2)}}}},$t={id:`reliability.tool-errors`,category:`reliability`,dataDeps:[`toolData`,`liveConfig`],rule(e){if(x(e.liveConfig?.settings))return null;let t=me(e.toolData).filter(e=>e.totalCalls>=5&&e.errorRate>=.2);return t.length===0?null:{id:`reliability.tool-errors`,category:`reliability`,severity:`warning`,title:`Tools with high error rates`,detail:`${t.length} tool(s) failed on ${(ge*100).toFixed(0)}%+ of calls. Failed calls waste a round-trip and often trigger retries.`,action:`Investigate the top offenders — common causes are stale file state, wrong paths, or missing permissions.`,affected:t.reduce((e,t)=>e+t.errorCalls,0),evidence:t.slice(0,5).map(e=>`${e.toolName}: ${(e.errorRate*100).toFixed(0)}% of ${e.totalCalls}`),view:`errors`,fix:{target:`hook`,label:`Add a post-edit typecheck hook`,note:`Merge into the "hooks" object in .claude/settings.json. Swap the command for your project’s real typecheck/lint so broken edits surface immediately instead of compounding.`,snippet:`{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "npm run -s typecheck" }
        ]
      }
    ]
  }
}`}}}},en={headings:[/^##\s+Rate-limit hygiene\b/i],bodyPhrases:[`serialize heavy automated batches`]},tn={id:`reliability.api-errors`,category:`reliability`,dataDeps:[`apiErrors`,`liveConfig`],rule(e){if(e.apiErrors.length===0)return null;let t=e.apiErrors.filter(e=>e.status!=null&&_e.has(String(e.status)));return t.length>0&&w(e.liveConfig,en)?null:t.length>0?{id:`reliability.rate-limits`,category:`reliability`,severity:`warning`,title:`Hitting rate-limit / overloaded errors`,detail:`${t.length} API error(s) returned 429/529 (rate limited or overloaded), which stall and retry turns.`,action:`Pace heavy automated runs, or move them to the priority service tier / off-peak hours.`,affected:t.length,view:`errors`,fix:{target:`CLAUDE.md`,label:`Add pacing guidance`,note:`Append to your project (or ~/.claude) CLAUDE.md so heavy runs self-throttle. 429/529 are server-side signals with no client retry/tier setting.`,snippet:`## Rate-limit hygiene\n\nHit ${t.length} rate-limit/overload (429/529) error(s) recently.\n- Avoid launching many parallel agent runs at once; serialize heavy automated batches.\n- Schedule large unattended runs (migrations, bulk refactors) for off-peak hours.\n- Break big jobs into smaller chunks with breaks between them rather than one long burst.`,appliedMarkers:en}}:{id:`reliability.api-errors`,category:`reliability`,severity:`info`,title:`API errors recorded`,detail:`${e.apiErrors.length} API error event(s) were logged across your sessions.`,action:`Open Errors to see the status-code breakdown and whether any pattern is recurring.`,affected:e.apiErrors.length,view:`errors`}}},nn={id:`reliability.retry-storms`,category:`reliability`,dataDeps:[`toolData`],rule(e){let t=pe(e.toolData).filter(e=>e.hasErrors&&e.count>=4);return t.length===0?null:{id:`reliability.retry-storms`,category:`reliability`,severity:`info`,title:`Retry storms on failing tools`,detail:`${t.length} run(s) of 4+ back-to-back same-tool calls included errors — the same operation retried repeatedly.`,action:`Check whether a different approach (or fixing the root error once) avoids the retry loop.`,affected:t.length,evidence:t.slice(0,5).map(e=>`${_(e.sessionId)}, ${e.toolName} ×${e.count}`),view:`errors`}}},rn={id:`reliability.settings-json-invalid`,category:`reliability`,dataDeps:[`liveConfig`],rule(e){let t=e.liveConfig?.settingsHealth;if(!t||!t.present||t.ok)return null;let n=t.findings.filter(e=>e.severity===`error`);return n.length===0?null:{id:`reliability.settings-json-invalid`,category:`reliability`,severity:`warning`,title:`settings.json has validation errors`,detail:`Your settings.json has ${n.length} error-severity finding(s) (syntax, type mismatch, or unknown key); Claude Code silently ignores an invalid file, so your model/permissions/hooks may have no effect.`,action:`Open Config Hygiene to see the exact bad path and expected type, then fix settings.json.`,affected:n.length,evidence:n.slice(0,5).map(e=>`${e.path}: ${e.message}`)}}},an=3,on={id:`reliability.hook-errors`,category:`reliability`,dataDeps:[`runtimeEvents`],rule(e){let t=e.runtimeEvents;if(!t||t.length===0)return null;let{errorEvents:n}=c(t);return n<an?null:{id:`reliability.hook-errors`,category:`reliability`,severity:`warning`,title:`Stop hooks are firing with errors`,detail:`Stop hooks errored on ${n} event(s) across your history; an erroring guard (lint, typecheck, git, notify) silently does nothing on every affected turn.`,action:`Inspect the failing hook command (visible in stop_hook_summary lines), fix it or add error handling so it does not fail silently.`,affected:n,view:`errors`}}},sn=5,cn={id:`reliability.hook-prevented-continuation`,category:`reliability`,dataDeps:[`runtimeEvents`],rule(e){let t=e.runtimeEvents;if(!t||t.length===0)return null;let{preventedContinuations:n}=c(t);return n<sn?null:{id:`reliability.hook-prevented-continuation`,category:`reliability`,severity:`warning`,title:`Stop hooks are blocking agent continuation`,detail:`${n} stop-hook event(s) returned a blocking exit, halting the session mid-run — most disruptive to unattended (sdk-*) workflows.`,action:`Review the blocking hook; if the block is unintentional, gate on the command exit code (or append "|| true") so it only blocks on real failures.`,affected:n,view:`errors`,fix:{target:`hook`,label:`Gate the hook on exit code`,note:`Restructure the Stop hook so it only blocks on a genuine failure. A hook blocks when its command exits non-zero; gate it (or append "|| true") so advisory output never halts the run.`,snippet:`{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "your-check.sh || true" }
        ]
      }
    ]
  }
}`}}}},ln=50,un={id:`reliability.dropped-assignments`,category:`reliability`,dataDeps:[`teams`],rule(e){let t=e.teams??[];if(!t.length)return null;let n=t.filter(e=>e.droppedCount>0);if(n.length===0)return null;let r=n.filter(e=>e.stalledAgents.length>0),i=n.filter(e=>e.droppedPct>=ln),a=r.length>0||i.length>0?`warning`:`info`,o=n.reduce((e,t)=>e+t.droppedCount,0),s=n.reduce((e,t)=>e+t.totalAssignments,0),c=n.flatMap(e=>e.stalledAgents.map(t=>`${e.teamId}/${t.agent}`)).slice(0,5);return{id:`reliability.dropped-assignments`,category:`reliability`,severity:a,title:`Task assignments silently dropped by worker agents`,detail:c.length>0?`${o}/${s} task assignment(s) dropped across ${n.length} team(s); stalled agents (never started): ${c.join(`, `)}.`:`${o}/${s} task assignment(s) dropped across ${n.length} team(s) — workers never acknowledged the work.`,action:`Re-dispatch the unread tasks or split a stalled agent's queue across fresh workers.`,affected:o,evidence:n.flatMap(e=>e.droppedAssignments.map(t=>`${e.teamId}/${t.agent} [${t.taskId}] "${t.subject}" (unread ${t.ageMinutes}m)`)).slice(0,5),fix:{target:`command`,label:`Redispatch unread tasks`,note:`Run for each affected team to re-queue the unacknowledged assignments.`,snippet:n.slice(0,3).map(e=>`claude-team redispatch --team ${e.teamId} --only-unread`).join(`
`)}}}},Q=e=>{let[t=0,n=0,r=0]=(e??`0.0.0`).split(`.`).map(e=>parseInt(e,10)||0);return[t,n,r]};function dn(e,t){let n=Q(e),r=Q(t),i=0;for(let e=0;e<3;e++)i+=Math.max(0,r[e]-n[e]);return i}var fn=e=>(e.outcome??e.status??`unknown`).toLowerCase(),pn=e=>fn(e)===`success`,$=864e5;function mn(e){if(e.length===0)return{grade:`A`,successRate:1,total:0,successCount:0,failedCount:0,cadenceDays:null,versionDrift:null,immediateRetries:0,errorCodes:[]};let t=[...e].sort((e,t)=>Date.parse(e.timestamp??`0`)-Date.parse(t.timestamp??`0`)),n=t.length,r=t.filter(pn),i=t.filter(e=>!pn(e)),a=n>0?r.length/n:1,o=null;if(n>1){let e=Date.parse(t[0].timestamp??`0`);o=(Date.parse(t[n-1].timestamp??`0`)-e)/$/(n-1)}let s;{let e=t[0].version_from,r=t[n-1].version_to,i=dn(e,r),a=n>1?Math.round((Date.parse(t[n-1].timestamp??`0`)-Date.parse(t[0].timestamp??`0`))/$):0;s=`${e??`?`} -> ${r??`?`} (${i} patch bumps over ${a}d)`}let c=0;for(let e=1;e<t.length;e++)Date.parse(t[e].timestamp??`0`)-Date.parse(t[e-1].timestamp??`0`)<$/24&&c++;let l=[...new Set(i.map(e=>e.error_code).filter(e=>!!e))];return{grade:a>=.95?`A`:a>=.85?`B`:a>=.7?`C`:`D`,successRate:a,total:n,successCount:r.length,failedCount:i.length,cadenceDays:o,versionDrift:s,immediateRetries:c,errorCodes:l}}var hn={id:`reliability.self-update-health`,category:`reliability`,dataDeps:[`updateResults`],rule(e){let t=e.updateResults??[];if(!t.length)return null;let n=mn(t);if(n.failedCount===0)return null;let r=`${(n.successRate*100).toFixed(0)}%`,i=n.errorCodes.length>0?n.errorCodes.join(`, `):`unknown error`,a=n.cadenceDays==null?`n/a (single record)`:`~1 update / ${n.cadenceDays.toFixed(1)}d`,o=n.successRate<.7?`warning`:`info`;return{id:`reliability.self-update-health`,category:`reliability`,severity:o,title:`CLI self-update health`,detail:`Across ${n.total} captured update snapshot(s), ${n.successCount} succeeded (${r}). ${n.failedCount} failed/blocked`+(n.errorCodes.length?` (${i})`:``)+`. ${n.immediateRetries} immediate retry(s). Cadence: ${a}. Version drift: ${n.versionDrift??`unknown`}.`,action:o===`warning`?`Pin a known-good version or investigate the failing update path (${i}) before trusting auto-update.`:`Some update attempts failed (${i}). Monitor for recurrence; pin a version if failures become frequent.`,affected:n.failedCount,view:`recommendations`}}};function gn(e,t={}){let n=[],r=[];for(let i of e.serversNeedingAuth){let a=e.entries[i],o=t[i]??0,s=o>0,c={name:i,reason:a?.reason??`unknown`,calls30d:o,blocking:s};s?n.push(c):r.push(c)}return n.sort((e,t)=>t.calls30d-e.calls30d),r.sort((e,t)=>t.calls30d-e.calls30d),{blocking:n,advisory:r}}function _n(e){let{attribution:t,sessions:n}=e;if(!t||t.length===0)return{};let r=new Set((n??[]).filter(e=>o(e.entrypoint)).map(e=>e.id));if(r.size===0)return{};let i={};for(let e of t)if(r.has(e.sessionId))for(let[t,n]of Object.entries(e.mcpServers))i[t]=(i[t]??0)+n.invocations;return i}function vn(e){return[`# Add to the top of your CI/cron entrypoint, before launching claude:`,`MCP_AUTH_STATUS=$(claude mcp auth --check 2>/dev/null; echo $?)`,`if [ "$MCP_AUTH_STATUS" = "1" ]; then`,`  echo "::error::MCP re-auth required (${e.join(`, `)}) — run claude mcp auth interactively, then retry"`,`  exit 1   # fail fast instead of a silent zero-progress run`,`fi`].join(`
`)}function yn(e){return e.map(e=>`claude mcp auth ${e}`).join(`
`)}var bn={id:`reliability.mcp-needs-auth`,category:`reliability`,dataDeps:[`attribution`,`sessions`],rule(e){let t=e.mcpAuth;if(!t||!t.serversNeedingAuth.length)return null;let{blocking:n,advisory:r}=gn(t,_n(e));if(n.length===0&&r.length===0)return null;let i=n.map(e=>e.name),a=r.map(e=>e.name),o=n.length+r.length,s=n.length>0?`critical`:`warning`,c;c=n.length>0&&r.length>0?`${n.length} blocking MCP server(s) (${i.join(`, `)}) and ${r.length} advisory server(s) (${a.join(`, `)}) need interactive re-auth. Unattended (CI/cron/sdk) sessions cannot complete an OAuth flow — every tool call to a blocking server will fail silently.`:n.length>0?`${n.length} load-bearing MCP server(s) (${i.join(`, `)}) need interactive re-auth. Unattended sessions cannot complete an OAuth flow — every tool call to these servers will fail, burning tokens with no progress.`:`${r.length} MCP server(s) (${a.join(`, `)}) need re-auth. These are not currently called by unattended sessions, but interactive runs will fail until re-authenticated.`;let l=n.length>0?`Run the re-auth commands interactively BEFORE your next automated run, then add the pre-flight GATE to your CI/cron entrypoint to fail fast on future expired tokens.\n\nRe-auth:\n${yn(i)}`:`Run \`claude mcp auth <server>\` interactively for each flagged server: ${a.join(`, `)}.`,u=n.length>0?{target:`command`,label:`Copy pre-flight GATE`,note:`Add to the top of your CI/cron entrypoint before launching claude.`,snippet:vn(i)}:void 0;return{id:`reliability.mcp-needs-auth`,category:`reliability`,severity:s,title:n.length>0?`${n.length} blocking MCP server(s) need re-auth — unattended runs will fail`:`${o} MCP server(s) need re-auth`,detail:c,action:l,affected:o,unattended:n.length>0,evidence:[...i,...a].map(e=>`claude mcp auth ${e}`),fix:u}}},xn=10080*60*1e3,Sn=1,Cn=[ke,je,Me,Ne,Ye,Xe,Ze,st,ct,lt,ut,dt,Yt,Xt,Zt,Qt,$t,tn,nn,{id:`activity.stale-projects`,category:`activity`,dataDeps:[`projects`,`liveConfig`],rule(e,t){if(typeof e.liveConfig?.settings?.cleanupPeriodDays==`number`)return null;let n=t-672*60*60*1e3,r=e.projects.filter(e=>e.sessionCount>=3&&e.lastSeen>0&&e.lastSeen<n);return r.length===0?null:(r.sort((e,t)=>e.lastSeen-t.lastSeen),{id:`activity.stale-projects`,category:`activity`,severity:`info`,title:`Projects have gone quiet`,detail:`${r.length} project(s) with 3+ sessions have had no activity in over 4 weeks.`,action:`If these are done, archive them; if not, they may be stalled and worth a check-in.`,affected:r.length,evidence:r.slice(0,5).map(e=>`${e.projectShort}, last ${Ee(e.lastSeen,t)}d ago`),view:`activity`,fix:{target:`settings.json`,label:`Auto-clean old transcripts`,note:`Add to ~/.claude/settings.json to age out idle history automatically. WARNING: this permanently deletes local transcripts older than the given days — including the data this dashboard reads. Default is 30; raise the number to keep more.`,snippet:`{
  "cleanupPeriodDays": 28
}`}})}},$e,Le,Ve,Ue,nt,Ge,rn,Je,on,cn,it,ot,bt,Tt,Dt,Ft,Vt,Ut,Wt,un,{id:`activity.activity-trend`,category:`activity`,dataDeps:[`statsCache`],rule(e,t){let n=e.statsCache;if(!n)return null;let r=he(n,t);if(r.verdict!==`hotter`||r.toolCallCount.pctChange<50)return null;let{pctChange:i,thisWeek:a,lastWeek:o}=r.toolCallCount,s=r.stale?` (data may be stale — run the CLI to refresh stats-cache.json)`:``;return{id:`activity.activity-trend`,category:`activity`,severity:i>=200?`warning`:`info`,title:`Tool-call rate is running ${i}% hotter than last week`,detail:`This week so far: ${a.toLocaleString()} tool calls vs ${o.toLocaleString()} last week (+${i}%).${s} At this pace you may hit the rolling 5-hour or weekly cap sooner than expected.`,action:`Consider cooling off — spread heavy work across more sessions, or check your 5h/weekly usage before starting the next large task.`,affected:a,evidence:[`tool calls this week: ${a.toLocaleString()}`,`tool calls last week: ${o.toLocaleString()}`,`sessions this week: ${r.sessionCount.thisWeek}`,`sessions last week: ${r.sessionCount.lastWeek}`,`source: stats-cache.json (lastComputedDate: ${n.lastComputedDate})`],view:`activity`}}},qt,Jt,hn,bn,{id:`reliability.config-drift`,category:`reliability`,dataDeps:[`configBackups`],rule(e,t){let n=e.configBackups??[];if(!n.length)return null;let r=t-xn,i=n.filter(e=>e.kind!==`global-churn`&&e.timestamp>=r);if(i.length<Sn)return null;let a=i.filter(e=>e.severity===`warning`),o=i.filter(e=>e.kind===`server-disabled`),s=a.length>0?`warning`:`info`,c=[...new Set(o.map(e=>e.server).filter(Boolean))],l=c.length?` MCP server(s) disabled for a project: ${c.join(`, `)}.`:``,u=[...new Set(i.map(e=>e.project).filter(Boolean))],d=a.sort((e,t)=>t.timestamp-e.timestamp).slice(0,5).map(e=>`${new Date(e.timestamp).toISOString().slice(0,16)+`Z`}${e.project?` [${e.project}]`:``}${e.server?` server=${e.server}`:``} ${e.kind} ${e.from??`?`} -> ${e.to??`?`}`);return{id:`reliability.config-drift`,category:`reliability`,severity:s,title:o.length?`MCP server silently disabled — tool may have stopped working`:`Project MCP/config drifted in the last 7 days`,detail:`${i.length} config-drift event(s) detected in the last 7 days across ${u.length} project(s).`+l+` ${a.length} event(s) need attention (trust flip, server disabled, blanket-enable on).`,action:o.length?`Re-enable the disabled server(s) in the affected project's MCP settings, or re-accept the trust dialog if the tool should still be active.`:`Review the flagged config changes in ~/.claude/backups/ to confirm they were intentional.`,affected:i.length,evidence:d,...u.length?{projects:u}:{}}}},{id:`reliability.agent-report-card`,category:`reliability`,dataDeps:[`sessionRegistry`,`telemetry`,`debugLogs`],rule(t){let n=e(t.sessionRegistry,t.telemetry,t.debugLogs);if(!n.projects.length)return null;let r=n.projects.filter(e=>e.verdict===`MOVE`),i=n.projects.filter(e=>e.verdict===`FLAG`);if(!r.length&&!i.length)return null;let a=r.length?`warning`:`info`,o=[...r,...i],s=o.slice(0,6).map(e=>`${D(e.cwd)||e.cwd}: ${e.verdict} — ${e.dominantEntrypoint} ${e.dominantShare}% / drag ${e.dragScore} (${e.dragBucket})`);return{id:`reliability.agent-report-card`,category:`reliability`,severity:a,title:`Agent Report Card: some projects aren’t a clean KEEP`,detail:`Joined attribution (sessions) with reliability (telemetry + debug) across ${n.totalProjects} project(s): ${n.tally.KEEP} KEEP, ${n.tally.FLAG} FLAG, ${n.tally.MOVE} MOVE. Fleet retry-storm rate ${n.fleetRetryStormPct}%, ${Math.round(n.fleetWastedMs/1e3)}s dead wall-clock. `+(r.length?`${r.length} project(s) carry heavy reliability drag despite committed attribution.`:`${i.length} project(s) need a reliability fix or a CLI decision before trusting their cost.`),action:`Open the Agent Report Card. For MOVE projects the CLI is actively expensive here; for FLAG, fix the reliability tax (or pick one entrypoint) before trusting per-task cost.`,affected:o.length,view:`report-card`,evidence:s}}}];function wn(e){return{...e,liveConfig:e.liveConfig??null,assistantFeatures:e.assistantFeatures??null}}var Tn=new WeakMap;function En(e,t){let n=t===void 0;if(n){let t=Tn.get(e);if(t)return t}let r=t??Date.now(),i=[];for(let t of Cn){let n=t.rule(e,r);n&&i.push(n)}let a=i.sort((e,t)=>{let n=h[t.severity]-h[e.severity];if(n!==0)return n;let r=(t.estSavingsUsd??0)-(e.estSavingsUsd??0);return r===0?(t.affected??0)-(e.affected??0):r});return n&&Tn.set(e,a),a}function Dn(e){return e.filter(e=>e.category===`cost`&&e.id!==`cost.expensive-sessions`).reduce((e,t)=>e+(t.estSavingsUsd??0),0)}export{W as a,vt as i,En as n,T as o,Dn as r,De as s,wn as t};